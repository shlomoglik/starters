import m from 'mithril'

let SetLeadType = (init) => {
    return {
        view: (vnode) => {
            return (
                m('.setGroup','setLaedType')
            )
        }
    }
}

module.exports = SetLeadType
